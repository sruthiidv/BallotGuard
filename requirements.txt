phe==1.5.0
requests>=2.31.0
pynacl>=1.5.0
pycryptodome>=3.19.0
opencv-python>=4.8.0
customtkinter>=5.1.3
flask>=2.3.0
numpy>=1.24.0
Pillow>=9.0.0
# face-recognition depends on dlib and system build tools; keep unpinned if building locally
face-recognition
cmake
dlib
ttkbootstrap>=1.10.1

# Notes:
# - If you have trouble installing `dlib`/`face-recognition`, prefer using a prebuilt wheel
#   or install with conda: `conda install -c conda-forge dlib face-recognition`.